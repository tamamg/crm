import * as ActionTypes from '../constants/ActionTypes';
import { List } from 'immutable'
let defaultState = {
  brandName: 'Recongate'
};

export function company(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
